export interface IPayload {
  sub: string;
  name: string;
  login_id: string;
}

export interface IGithubUserTypes {
  login_id: string;
  profile_image: string;
  name: string;
  about_me: string;
}
