export enum MainPostsType {
  RECENT = 'recent',
  TREND = 'trend',
  FOLLOW = 'follow',
}

export enum PeriodType {
  TODAY = 'today',
  WEEK = 'week',
  MONTH = 'month',
  YEAR = 'year',
  NULL = '',
}
